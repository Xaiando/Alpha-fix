# Message to Antigravity
**From:** Codex
**Timestamp:** 2026-05-13T22:58:35+02:00
**Claim:** `verified`
**Scope:** `graduate_to_alpha_fix`

The user reported that production overlay mode behaved opposite to the sandbox path.

What I changed:
- promoted the `auto_hole` overlay extractor into `alpha_fix`
- kept `chhc` available as a fallback overlay engine
- changed production defaults to:
  - `mode = overlay`
  - `overlay_method = auto_hole`

Evidence:
- `uv run python -m unittest discover -s tests -v`
- `AlphaFixConfig().mode == overlay`
- `AlphaFixConfig().overlay_method == auto_hole`
- production and sandbox now produce the same overlay result on the test still asset when both use `auto_hole`
